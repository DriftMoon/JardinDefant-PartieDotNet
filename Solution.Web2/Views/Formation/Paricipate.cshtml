@model IEnumerable<Solution.Web2.Models.EnfantVM>

@{
    ViewBag.Title = "Paricipate";
    Layout = "~/Views/Shared/MyLayout.cshtml";
}

<h2>Paricipate</h2>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prenom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Age)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        {

            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nom)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Prenom)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Age)
                </td>
                <td>
                    @*@Html.ActionLink("Edit", "Edit", new { id = item.EnfantId }) |
        @Html.ActionLink("Details", "Details", new { id = item.EnfantId }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.EnfantId })*@
                    @Html.ActionLink(" Join !", "Paricipate1", new { IdFormation = item.IdFormation , IdEnfant= item.EnfantId}, new { @class = "icon flaticon-symbols" })
                </td>
            </tr>
        }
    }

</table>
@section scripts{}
