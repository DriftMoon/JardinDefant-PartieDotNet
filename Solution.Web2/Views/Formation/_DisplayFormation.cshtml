@model Solution.Web2.Models.FormationVM
@{string path = "~/Content/Uploads/" + @Model.Title+"thumb.jpg";}

@*<div class="event-item " style="flex:1; padding: 16px;">
        <div class="event-image">
            <img src="@Url.Content(path)" alt="Affiche" class="img-responsive" />
            <div class="date">
                <span>@Model.Start.Day.</span>
                <p>@Model.Start.Month</p>
            </div>
        </div>
        <div class="event-content" style="width:348px ; max-height:200px">
            <h4>@Model.Title</h4>
            <ul>
                <li><span><i class="fa fa-clock-o" aria-hidden="true"></i></span>@Model.Start - @Model.End</li>
                <li><span><i class="fa fa-home" aria-hidden="true"></i></span> @Model.Location</li>
                <li><span><i class="fa fa-dollar" aria-hidden="true"></i></span> @Model.Price Dt</li>
            </ul>
            <p style="width:250px;white-space: nowrap;overflow: hidden;text-overflow: ellipsis;">@Model.Description</p>
            @Html.ActionLink("Join Now", "Details", new { id = Model.FormationID })
            @Html.ActionLink("Join Now", "Details", new { id = Model.FormationID },new{ @class = "button-default" })
        </div>
    </div>*@

<!-- event blog -->
<!-- event End here -->
<div class="col-lg-4 col-sm-6 col-xs-12">
    <div class="event-item">
        <div class="event-image">
            <img src="@Url.Content(path)" alt="event image" class="img-responsive" intrinsicsize="250x250">
            <div class="date">
                <span>@Model.Start.Day.</span>
                <p>@Model.Start.Month</p>
            </div>
        </div>
        <div class="event-content">
            <h4>@Model.Title</h4>
            <ul>
                <li><span><i class="fa fa-clock-o" aria-hidden="true"></i></span>@Model.Start - @Model.End.Date</li>
                <li><span><i class="fa fa-home" aria-hidden="true"></i></span> @Model.Location</li>
                <li><span><i class="fa fa-dollar" aria-hidden="true"></i></span> @Model.Price Dt</li>
            </ul>
            <p style="width:250px;white-space: nowrap;overflow: hidden;text-overflow: ellipsis;">@Model.Description</p>
            @Html.ActionLink("Join Now", "Details", new { id = Model.FormationID }, new { @class = "button-default" })
        </div>
    </div>
</div>

@section scripts{}
